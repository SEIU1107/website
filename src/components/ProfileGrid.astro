---
import Placeholder from '../images/PlaceholderProfileImg.png'

export interface Props {
    data: {
        name: string;
        profileImg: string; /* Picture of the Steward, if any. Otherwise, a placeholder is put instead */
        [key: string]: string;
    }[];
    className: string;
}

const {data, className = "card-grid"} = Astro.props as Props;

---


<div class={className}>
    {data.map((person) => (
            <div class="card">
                <img src={person.profileImg ? person.profileImg : Placeholder} alt="" />
                <h2 class="name">
                    <strong>{person.name}</strong>
                </h2>
                <div class="info">
                    {Object.keys(person).map((key) => (
                        key !== 'profileImg' &&
                        key !== 'name' &&
                        key !== 'id' && (
                            key === 'Email' ? (
                                    <div>
                                        <b>{key}:</b> <a href={`mailto:${person[key]}`}>{person[key]}</a>
                                    </div>
                            ) : (
                                    <div>
                                        <b>{key}:</b> {person[key]}
                                    </div>
                            )
                        )
                    ))}
                </div>
            </div>
    ))}
</div>


<style>
    .card-grid {
        display: grid;
        grid-template-columns: repeat(4,1fr);
        grid-column-gap: 0;
        margin: auto;
        justify-content: center;
        justify-items: center;
        max-width: 375px;
        padding: 10px;
        font-family: 'Roboto', sans-serif;
    }

    .card {
        display: flex;
        flex-direction: column;
        background: #fff;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin: 20px;
    }

    .info {
        display: flex;
        justify-content: center;
        flex-direction: column;
        font-family: Verdana, sans-serif;
    }
    .name {
        font-family: Georgia, sans-serif;
        font-weight: bold;
        color: #000;
        padding-bottom: 10px;
        padding-top: 10px;
    }
    img {
        max-width: 300px;
        border-radius: 5%;
    }

    @media (max-width: 1400px) {
        .card-grid {
            grid-template-columns: repeat(3,1fr);
        }
    }

    @media (max-width: 767px) {
        .card-grid {
            grid-template-columns: 1fr;
        }
    }


</style>